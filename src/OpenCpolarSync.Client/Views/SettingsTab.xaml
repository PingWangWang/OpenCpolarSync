<UserControl x:Class="OpenCpolarSync.Client.Views.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600"
             Background="{DynamicResource BgSecondary}">
    <Border Padding="28,20" Background="{DynamicResource BgSecondary}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- 启动设置 -->
                <Border Style="{StaticResource CardStaticStyle}" Padding="0" Margin="0,0,0,12">
                    <StackPanel>
                        <Border Padding="20,16,20,12" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="4" Height="20" Background="{DynamicResource BrandBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                                <TextBlock Text="启动设置" FontSize="15" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Grid Margin="20,16,20,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="开机自动启动" FontSize="13.5" FontWeight="600" Foreground="{DynamicResource TextPrimary}"/>
                                <TextBlock Text="登录 Windows 后自动启动 OpenCpolarSync（当前用户级，无需管理员权限）" FontSize="11.5" Foreground="{DynamicResource TextTertiary}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkAutoStart" Grid.Column="1" VerticalAlignment="Center" Checked="ChkAutoStart_Checked" Unchecked="ChkAutoStart_Unchecked"/>
                        </Grid>

                        <Border Height="1" Background="{DynamicResource BorderLightBrush}" Margin="20,0,20,0"/>

                        <Grid Margin="20,14,20,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="自启后自动启动守护" FontSize="13.5" FontWeight="600" Foreground="{DynamicResource TextPrimary}"/>
                                <TextBlock Text="程序开机启动后，自动开始监控 Cpolar 和 Openlist 服务" FontSize="11.5" Foreground="{DynamicResource TextTertiary}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                            <CheckBox x:Name="ChkAutoStartGuard" Grid.Column="1" VerticalAlignment="Center" Checked="ChkAutoStartGuard_Checked" Unchecked="ChkAutoStartGuard_Unchecked"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 外观设置 -->
                <Border Style="{StaticResource CardStaticStyle}" Padding="0" Margin="0,0,0,12">
                    <StackPanel>
                        <Border Padding="20,16,20,12" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="4" Height="20" Background="{DynamicResource InfoBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                                <TextBlock Text="外观设置" FontSize="15" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <StackPanel Margin="20,16,20,20">
                            <TextBlock Text="主题模式" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbTheme" SelectionChanged="CmbTheme_SelectionChanged" Margin="0,0,0,14">
                                <ComboBoxItem Content="浅色" Tag="Light"/>
                                <ComboBoxItem Content="深色" Tag="Dark"/>
                                <ComboBoxItem Content="跟随系统" Tag="Auto"/>
                            </ComboBox>

                            <TextBlock Text="背景效果" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbBackgroundEffect" SelectionChanged="CmbBackgroundEffect_SelectionChanged">
                                <ComboBoxItem Content="Mica 云母（推荐 Win11）" Tag="Mica"/>
                                <ComboBoxItem Content="MicaAlt 云母变体" Tag="MicaAlt"/>
                                <ComboBoxItem Content="Acrylic 亚克力（Win10+）" Tag="Acrylic"/>
                                <ComboBoxItem Content="无（纯色）" Tag="None"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 关闭行为 -->
                <Border Style="{StaticResource CardStaticStyle}" Padding="0" Margin="0,0,0,12">
                    <StackPanel>
                        <Border Padding="20,16,20,12" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="4" Height="20" Background="{DynamicResource WarningBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                                <TextBlock Text="关闭行为" FontSize="15" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <StackPanel Margin="20,16,20,20">
                            <TextBlock Text="关闭窗口时" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="CmbCloseMode" SelectionChanged="CmbCloseMode_SelectionChanged">
                                <ComboBoxItem Content="每次询问" Tag="Ask"/>
                                <ComboBoxItem Content="最小化到托盘" Tag="Tray"/>
                                <ComboBoxItem Content="直接退出程序" Tag="Quit"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 提示信息 -->
                <Border Background="{DynamicResource InfoBgBrush}" CornerRadius="10" Padding="16,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="ⓘ" FontSize="15" Foreground="{DynamicResource InfoBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="设置即时生效。背景效果需 Win10+，Win7 自动降级为纯色。卸载时会自动清理开机自启动项。" FontSize="12" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
