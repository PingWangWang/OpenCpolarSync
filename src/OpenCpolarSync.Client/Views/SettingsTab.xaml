<UserControl x:Class="OpenCpolarSync.Client.Views.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600"
             Background="{DynamicResource BgSecondary}">
    <Border Padding="24" Background="{DynamicResource BgSecondary}">
        <StackPanel>
            <TextBlock Text="管理程序的开机自启动等全局行为设置" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

            <!-- 启动设置 -->
            <Border Style="{StaticResource CardStaticStyle}" Margin="0,0,0,8">
                <StackPanel>
                    <DockPanel Margin="0,0,0,8">
                        <Border Width="4" Height="18" Background="{DynamicResource BrandBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                        <TextBlock Text="启动设置" FontSize="14" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </DockPanel>

                    <!-- 开机自启动 -->
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="开机自动启动" FontSize="13" FontWeight="600" Foreground="{DynamicResource TextPrimary}"/>
                            <TextBlock Text="登录 Windows 后自动启动 OpenCpolarSync（当前用户级，无需管理员权限）" FontSize="11" Foreground="{DynamicResource TextTertiary}" Margin="0,2,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                        <CheckBox x:Name="ChkAutoStart" Grid.Column="1" VerticalAlignment="Center" Checked="ChkAutoStart_Checked" Unchecked="ChkAutoStart_Unchecked"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 提示信息 -->
            <Border Background="{DynamicResource InfoBgBrush}" CornerRadius="8" Padding="12,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ⓘ" FontSize="14" Foreground="{DynamicResource InfoBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="修改即时生效，无需重启程序。卸载时会自动清理开机自启动项。" FontSize="12" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
