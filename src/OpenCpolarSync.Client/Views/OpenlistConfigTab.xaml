<UserControl x:Class="OpenCpolarSync.Client.Views.OpenlistConfigTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600"
             Background="{DynamicResource BgSecondary}">
    <Border Padding="28,20" Background="{DynamicResource BgSecondary}">
        <StackPanel>
            <!-- Openlist 管理卡片 -->
            <Border Style="{StaticResource CardStaticStyle}" Padding="0" Margin="0,0,0,12">
                <StackPanel>
                    <Border Padding="20,16,20,12" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="4" Height="20" Background="{DynamicResource BrandBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                            <TextBlock Text="Openlist 管理" FontSize="15" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <StackPanel Margin="20,16,20,20">
                        <TextBlock Text="管理用户名" Style="{StaticResource FormLabel}"/>
                        <TextBox Text="admin" IsReadOnly="True" Background="{DynamicResource BgTertiary}" Margin="0,0,0,14"/>

                        <TextBlock Text="登录密码" Style="{StaticResource FormLabel}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0">
                                <PasswordBox x:Name="TxtOpenlistPassword" PasswordChanged="TxtOpenlistPassword_PasswordChanged"/>
                                <TextBox x:Name="TxtOpenlistPasswordVisible" Visibility="Collapsed" TextChanged="TxtOpenlistPasswordVisible_TextChanged"/>
                            </Grid>
                            <CheckBox x:Name="ChkOpenlistShowPassword" Grid.Column="1" Content="显示密码" VerticalAlignment="Center" Margin="12,0,0,0" Checked="ChkOpenlistShowPassword_Checked" Unchecked="ChkOpenlistShowPassword_Unchecked"/>
                        </Grid>

                        <Border Background="{DynamicResource InfoBgBrush}" CornerRadius="8" Padding="12,10" Margin="0,14,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="ⓘ" FontSize="14" Foreground="{DynamicResource InfoBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="管理员用户名固定为 admin，不可修改；此处填写的密码将用于设置其登录密码。保存且 openlist 未运行时自动应用。" FontSize="11.5" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 操作按钮 -->
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <Button x:Name="BtnSave" Content="保存配置" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0" Click="BtnSave_Click"/>
                <Button x:Name="BtnReset" Content="重置" Style="{StaticResource SecondaryButton}" Click="BtnReset_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
