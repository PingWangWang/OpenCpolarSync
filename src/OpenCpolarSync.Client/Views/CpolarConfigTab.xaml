<UserControl x:Class="OpenCpolarSync.Client.Views.CpolarConfigTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600"
             Background="{DynamicResource BgSecondary}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24">
        <StackPanel>
            <TextBlock Text="配置 Cpolar 隧道监控的连接参数与钉钉推送通知" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

            <!-- 基础配置 -->
            <Border Style="{StaticResource CardStaticStyle}" Margin="0,0,0,8">
                <StackPanel>
                    <!-- 分组标题 -->
                    <DockPanel Margin="0,0,0,8">
                        <Border Width="4" Height="18" Background="{DynamicResource BrandBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                        <TextBlock Text="基础配置" FontSize="14" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </DockPanel>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="钉钉 Webhook 地址" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBox Grid.Column="1" Text="{Binding WebhookUrl, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="安全关键词" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBox Grid.Column="1" Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="轮询间隔（分钟）" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBox Grid.Column="1" Text="{Binding Interval, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="监控隧道名称" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedTunnelNamesText, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Cpolar 连接配置 -->
            <Border Style="{StaticResource CardStaticStyle}" Margin="0,0,0,8">
                <StackPanel>
                    <DockPanel Margin="0,0,0,8">
                        <Border Width="4" Height="18" Background="{DynamicResource InfoBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                        <TextBlock Text="Cpolar 连接" FontSize="14" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </DockPanel>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="API 地址" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBox Grid.Column="1" Text="{Binding CpolarApiBase, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="登录邮箱" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                        <TextBox Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <!-- 登录密码 + 显示密码 -->
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="登录密码" VerticalAlignment="Center" FontSize="13" Foreground="{DynamicResource TextSecondary}"/>
                        <Grid Grid.Column="1">
                            <PasswordBox x:Name="TxtPassword" PasswordChanged="TxtPassword_PasswordChanged"/>
                            <TextBox x:Name="TxtPasswordVisible" Visibility="Collapsed" TextChanged="TxtPasswordVisible_TextChanged"/>
                        </Grid>
                        <CheckBox x:Name="ChkShowPassword" Grid.Column="2" Content="显示" VerticalAlignment="Center" Margin="10,0,0,0" Checked="ChkShowPassword_Checked" Unchecked="ChkShowPassword_Unchecked"/>
                    </Grid>

                    <CheckBox Content="调试模式（输出详细日志）" IsChecked="{Binding Debug}" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- 操作按钮 -->
            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                <Button x:Name="BtnSave" Content="保存配置" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0" Click="BtnSave_Click"/>
                <Button x:Name="BtnReset" Content="重置" Style="{StaticResource SecondaryButton}" Click="BtnReset_Click"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
