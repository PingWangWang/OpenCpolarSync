<UserControl x:Class="OpenCpolarSync.Client.Views.CpolarConfigTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600"
             Background="{DynamicResource BgSecondary}">
    <Border Padding="28,20" Background="{DynamicResource BgSecondary}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- 基础配置卡片 -->
                <Border Style="{StaticResource CardStaticStyle}" Padding="0" Margin="0,0,0,12">
                    <StackPanel>
                        <Border Padding="20,16,20,12" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="4" Height="20" Background="{DynamicResource BrandBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                                <TextBlock Text="基础配置" FontSize="15" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <StackPanel Margin="20,16,20,20">
                            <TextBlock Text="钉钉 Webhook 地址" Style="{StaticResource FormLabel}"/>
                            <TextBox Text="{Binding WebhookUrl, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14"/>

                            <TextBlock Text="安全关键词" Style="{StaticResource FormLabel}"/>
                            <TextBox Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="轮询间隔（分钟）" Style="{StaticResource FormLabel}"/>
                                    <TextBox Text="{Binding Interval, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="监控隧道名称" Style="{StaticResource FormLabel}"/>
                                    <TextBox Text="{Binding SelectedTunnelNamesText, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Cpolar 连接配置卡片 -->
                <Border Style="{StaticResource CardStaticStyle}" Padding="0" Margin="0,0,0,12">
                    <StackPanel>
                        <Border Padding="20,16,20,12" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="4" Height="20" Background="{DynamicResource InfoBrush}" CornerRadius="2" VerticalAlignment="Center"/>
                                <TextBlock Text="Cpolar 连接" FontSize="15" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <StackPanel Margin="20,16,20,20">
                            <TextBlock Text="API 地址" Style="{StaticResource FormLabel}"/>
                            <TextBox Text="{Binding CpolarApiBase, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14"/>

                            <TextBlock Text="登录邮箱" Style="{StaticResource FormLabel}"/>
                            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14"/>

                            <TextBlock Text="登录密码" Style="{StaticResource FormLabel}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0">
                                    <PasswordBox x:Name="TxtPassword" PasswordChanged="TxtPassword_PasswordChanged"/>
                                    <TextBox x:Name="TxtPasswordVisible" Visibility="Collapsed" TextChanged="TxtPasswordVisible_TextChanged"/>
                                </Grid>
                                <CheckBox x:Name="ChkShowPassword" Grid.Column="1" Content="显示密码" VerticalAlignment="Center" Margin="12,0,0,0" Checked="ChkShowPassword_Checked" Unchecked="ChkShowPassword_Unchecked"/>
                            </Grid>

                            <CheckBox Content="调试模式（输出详细日志）" IsChecked="{Binding Debug}" Margin="0,14,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 操作按钮 -->
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <Button x:Name="BtnSave" Content="保存配置" Style="{StaticResource PrimaryButton}" Margin="0,0,10,0" Click="BtnSave_Click"/>
                    <Button x:Name="BtnReset" Content="重置" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="BtnReset_Click"/>
                    <Button x:Name="BtnExport" Content="导出配置" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="BtnExport_Click"/>
                    <Button x:Name="BtnImport" Content="导入配置" Style="{StaticResource SecondaryButton}" Click="BtnImport_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Border>
</UserControl>
