<UserControl x:Class="OpenCpolarSync.Client.Views.DashboardTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700"
             Background="{DynamicResource BgSecondary}">
    <Border Padding="28,20" Background="{DynamicResource BgSecondary}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 服务卡片：两列布局 -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Cpolar 卡片 -->
                <Border Grid.Column="0" Style="{StaticResource CardStaticStyle}" Padding="0">
                    <StackPanel>
                        <!-- 卡片头部 -->
                        <Border Padding="20,18,20,14" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <DockPanel>
                                <Border Width="42" Height="42" CornerRadius="10" Background="{DynamicResource InfoBgBrush}" VerticalAlignment="Center">
                                    <Path Data="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5" Stretch="Uniform" Width="22" Height="22" Fill="{DynamicResource InfoBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="Cpolar 隧道监控" FontSize="15" FontWeight="700" Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="localhost:9200" FontSize="11" Foreground="{DynamicResource TextTertiary}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <Border DockPanel.Dock="Right" Background="{DynamicResource SuccessBgBrush}" CornerRadius="12" Padding="10,5" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="7" Height="7" Fill="{DynamicResource SuccessBrush}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding CpolarStatusText}" FontSize="11" FontWeight="600" Foreground="{DynamicResource SuccessBrush}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </DockPanel>
                        </Border>

                        <!-- 数据区 -->
                        <Grid Margin="20,16,20,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="监控隧道" FontSize="11" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="{Binding CpolarTunnelCount, StringFormat='{}{0} 个'}" FontSize="20" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="进程 PID" FontSize="11" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="{Binding CpolarProcessId, StringFormat='{}{0}'}" FontSize="20" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="最近推送" FontSize="11" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="{Binding LastPushTime}" FontSize="14" Foreground="{DynamicResource TextSecondary}" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Grid>

                        <!-- 操作区 -->
                        <Border Padding="20,0,20,18">
                            <Button x:Name="BtnOpenCpolarWeb" Content="打开 Web 管理" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Stretch" Click="BtnOpenCpolarWeb_Click"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Openlist 卡片 -->
                <Border Grid.Column="2" Style="{StaticResource CardStaticStyle}" Padding="0">
                    <StackPanel>
                        <Border Padding="20,18,20,14" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,0,0,1">
                            <DockPanel>
                                <Border Width="42" Height="42" CornerRadius="10" Background="{DynamicResource BrandLightBrush}" VerticalAlignment="Center">
                                    <Path Data="M10 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V8c0-1.1-.9-2-2-2h-8l-2-2z" Stretch="Uniform" Width="22" Height="22" Fill="{DynamicResource BrandBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="Openlist 文件服务" FontSize="15" FontWeight="700" Foreground="{DynamicResource TextPrimary}"/>
                                    <TextBlock Text="localhost:5244" FontSize="11" Foreground="{DynamicResource TextTertiary}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <Border DockPanel.Dock="Right" Background="{DynamicResource SuccessBgBrush}" CornerRadius="12" Padding="10,5" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="7" Height="7" Fill="{DynamicResource SuccessBrush}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding OpenlistStatusText}" FontSize="11" FontWeight="600" Foreground="{DynamicResource SuccessBrush}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </DockPanel>
                        </Border>

                        <Grid Margin="20,16,20,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="进程 PID" FontSize="11" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="{Binding OpenlistProcessId, StringFormat='{}{0}'}" FontSize="20" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="运行状态" FontSize="11" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="持续监控中" FontSize="14" Foreground="{DynamicResource TextSecondary}" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Border Padding="20,0,20,18">
                            <Button x:Name="BtnOpenOpenlistWeb" Content="打开 Web 管理" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Stretch" Click="BtnOpenOpenlistWeb_Click"/>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- 底部提示信息 -->
            <Border Grid.Row="2" Background="{DynamicResource InfoBgBrush}" CornerRadius="10" Padding="16,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ⓘ" FontSize="15" Foreground="{DynamicResource InfoBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="点击「启动全部守护」开始监控。守护进程会在后台持续运行，关闭窗口后可通过托盘图标重新打开。" FontSize="12.5" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
