<UserControl x:Class="OpenCpolarSync.Client.Views.DashboardTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="700"
             Background="{DynamicResource BgSecondary}">
    <ScrollViewer x:Name="PageScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24,16">
        <Grid MinHeight="{Binding ActualHeight, ElementName=PageScroll}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <!-- 页面说明 -->
                <TextBlock Text="实时监控 Cpolar 隧道与 Openlist 文件服务的运行状态" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"/>

            <!-- 服务卡片：固定两列布局，确保始终在同一行 -->
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Cpolar 卡片 -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <!-- 标题行 -->
                        <DockPanel Margin="0,0,0,14">
                            <Border Width="36" Height="36" CornerRadius="8" Background="{DynamicResource InfoBgBrush}" VerticalAlignment="Center">
                                <TextBlock Text="⬡" FontSize="18" Foreground="{DynamicResource InfoBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Cpolar 隧道监控" FontSize="14" FontWeight="700" Foreground="{DynamicResource TextPrimary}"/>
                                <TextBlock Text="localhost:9200" FontSize="11" Foreground="{DynamicResource TextTertiary}" Margin="0,1,0,0"/>
                            </StackPanel>
                            <!-- 状态徽章 -->
                            <Border DockPanel.Dock="Right" Background="{DynamicResource SuccessBgBrush}" CornerRadius="20" Padding="8,4" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="6" Height="6" Fill="{DynamicResource SuccessBrush}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding CpolarStatusText}" FontSize="11" FontWeight="600" Foreground="{DynamicResource SuccessBrush}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </DockPanel>

                        <!-- 数据行 -->
                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="监控隧道" FontSize="10" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="{Binding CpolarTunnelCount, StringFormat='{}{0} 个'}" FontSize="16" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="进程 PID" FontSize="10" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="{Binding CpolarProcessId, StringFormat='{}{0}'}" FontSize="16" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="最近推送" FontSize="10" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="{Binding LastPushTime}" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Border Height="1" Background="{DynamicResource BorderLightBrush}" Margin="0,0,0,14"/>

                        <!-- 操作按钮：仅保留 Web 管理 -->
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BtnOpenCpolarWeb" Content="打开 Web 管理" Style="{StaticResource PrimaryButton}" Click="BtnOpenCpolarWeb_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Openlist 卡片 -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <!-- 标题行 -->
                        <DockPanel Margin="0,0,0,14">
                            <Border Width="36" Height="36" CornerRadius="8" Background="{DynamicResource BrandLightBrush}" VerticalAlignment="Center">
                                <TextBlock Text="◈" FontSize="18" Foreground="{DynamicResource BrandBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Openlist 文件服务" FontSize="14" FontWeight="700" Foreground="{DynamicResource TextPrimary}"/>
                                <TextBlock Text="localhost:5244" FontSize="11" Foreground="{DynamicResource TextTertiary}" Margin="0,1,0,0"/>
                            </StackPanel>
                            <!-- 状态徽章 -->
                            <Border DockPanel.Dock="Right" Background="{DynamicResource SuccessBgBrush}" CornerRadius="20" Padding="8,4" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="6" Height="6" Fill="{DynamicResource SuccessBrush}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding OpenlistStatusText}" FontSize="11" FontWeight="600" Foreground="{DynamicResource SuccessBrush}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </DockPanel>

                        <!-- 数据行 -->
                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="进程 PID" FontSize="10" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="{Binding OpenlistProcessId, StringFormat='{}{0}'}" FontSize="16" FontWeight="700" Foreground="{DynamicResource TextPrimary}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="运行状态" FontSize="10" FontWeight="600" Foreground="{DynamicResource TextTertiary}"/>
                                <TextBlock Text="持续监控中" FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>

                        <Border Height="1" Background="{DynamicResource BorderLightBrush}" Margin="0,0,0,14"/>

                        <!-- 操作按钮：仅保留 Web 管理 -->
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BtnOpenOpenlistWeb" Content="打开 Web 管理" Style="{StaticResource PrimaryButton}" Click="BtnOpenOpenlistWeb_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</UserControl>
